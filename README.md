# Stage Zero

A responsive React marketplace connecting early-stage SaaS founders with beta testers. Built with Vite, Tailwind CSS, and Lucide icons.

```bash
npm install
npm run build
npm start
```

The production server binds to `0.0.0.0`, honors `PORT`, and exposes `/health`.
